All data used in this `api-testing` folder are for demonstration only — NO REAL user or production data are used.

The tests and requests were executed in Coursera's isolated sandbox environment (a temporary, isolated execution environment). All APIs and responses shown here are mock/sample data and do not represent real users, accounts, or production systems.

Verification:
- See the `curl/` folder for curl command samples and related screenshots.
- See the `postman/` folder for Postman request screenshots and examples.

Purpose:
This disclaimer is included so reviewers, potential employers, or other parties understand that no real personal or sensitive data are exposed by the files in this folder.